# Pactflow Broker configuration
# Copy file này thành .env và điền giá trị thật
# KHÔNG commit file .env — chỉ commit file này

PACT_BROKER_BASE_URL=https://<your-workspace>.pactflow.io
PACT_BROKER_TOKEN=<your-api-token>
